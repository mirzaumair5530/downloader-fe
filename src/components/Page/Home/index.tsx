import { FC, PropsWithChildren } from "react";
import UrlSearch from "../../sub-components/UrlSearch";

const HomeComponent: FC<PropsWithChildren> = () => {
	return <UrlSearch />;
};

export default HomeComponent;
